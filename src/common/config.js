const configPath = {
  /**
   * 执行build 时候 ，数据接口是测试环境还是线上环境
   * @type {String}
   */
  buildEnv: 'testing',
  /**
   * 是否使用mock数据
   * @type {Boolean}
   */
  mock: false
}

export default configPath
